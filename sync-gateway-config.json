{
    "log":["CRUD+", "REST+", "Changes+", "Attach+"],
    "databases": {
        "mydb": {                        
            "sync":"function (doc) {channel (doc.channels);}",
            "users": {
                "GUEST": {
                    "disabled": false,
                    "admin_channels": ["*"]
                }
            }
        }
    } 
}